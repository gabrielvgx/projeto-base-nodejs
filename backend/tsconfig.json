{
  "compilerOptions": {
    "baseUrl": ".",

    // Layout de arquivos
    "rootDir": "./src",
    "outDir": "./dist",

    // Ambiente - Node.js LTS (v22+) com ESModules
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "target": "es2023",
    "lib": ["es2023"],
    "types": ["node"],

    // Outputs auxiliares
    "sourceMap": false,
    "declaration": false,
    "declarationMap": false,

    // Tipagem estrita
    "strict": true, // Habilita um conjunto de verificações estritas
    "noUncheckedIndexedAccess": true, // arr[0] pode ser undefined
    "exactOptionalPropertyTypes": true, // Distingue `prop?: string` de `prop: string | undefined`
    "noUncheckedSideEffectImports": true, // Imports sem binding são verificados

    // Boas práticas - evita código morto e descuido
    "noUnusedLocals": true, // Erro em variáveis locais não usadas
    "noUnusedParameters": true, // Erro em parâmetros não usados
    "noImplicitReturns": true, // Toda branch de uma função deve retornar valor
    "noFallthroughCasesInSwitch": true, // Evita cases sem break/return acidentais
    "noImplicitOverride": true, // Obriga uso explícito de `override` em subclasses

    // Compatibilidade e interop
    "verbatimModuleSyntax": true, // Garante `import type` onde necessário
    "isolatedModules": true, // Compatível com bundlers e transpiladores
    "moduleDetection": "force", // Trata todos os arquivos como módulos
    "skipLibCheck": true, // Não valida .d.ts de dependências (mais rápido)
    "esModuleInterop": true, // Facilita imports de libs CommonJS (ex: `import fs from 'fs'`)

    "paths": {
      "@types/*": ["src/@types/*"],
      "@interface/*": ["src/interfaces/*"],
      "@middlewares/*": ["src/middlewares/*"],
      "@validations/*": ["src/validations/*"],
      "@routes/*": ["src/routes/*"],
      "@controllers/*": ["src/controllers/*"],
      "@services/*": ["src/services/*"],
      "@utils/*": ["src/utils/*"]
    },
    "typeRoots": ["./node_modules/@types", "./src/@types"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}

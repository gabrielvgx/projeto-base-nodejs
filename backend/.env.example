DATABASE_URL="" # SQLite: "file:./prisma/dev.db" | supabase-postgresql postgresql://user.projectId:password@host:port/dbname?pgbouncer=true
SECRET_KEY="" # Secret Key to use in JWT token generation, should be a long random string for security
SRV_PORT=3000 # Server port

# Mail
MAIL_PROVIDER="" # GMAIL | GENERIC
MAIL_FROM="" # From email user
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USER=
MAIL_PASS=
MAIL_SECURE=false

# Gmail OAuth2.0
MAIL_ID="" # Client Id
MAIL_SECRET="" # Client Secret
MAIL_REFRESH_TOKEN="" # Refresh Token